# grader-app

Streamlit app for reviewing transcript differences and saving per-user annotations to Google Sheets.

## Run locally

```bash
pip install -r requirements.txt
streamlit run grader_filtered.py